<div class="container">
  <div class="row">
    <% @idols.each do |idol| %>
      <div class="col-sm-4">
        <%# <div class="card-product" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%= cl_image_path idol.photo.key, height: 300, width: 400, crop: :fill %>
        <%= link_to idol_path(idol), class: "text-decoration-none link-dark col-4" do %>
          <div class="card-product p-0 m-3">
            <img src="https://source.unsplash.com/random/700x600/?<%= idol.name %>"/>
            <div class="card-product-infos h-100">
              <h2><%= idol.name %></h2>
              <h3><%= idol.power %></h3>
              <p><%= idol.description %> with <strong>relevant info</strong> only.</p>

              <%# </div> %>
            </div>
          </div>
        <% end %>
      </div>
    <% end %>
  </div>
</div>
